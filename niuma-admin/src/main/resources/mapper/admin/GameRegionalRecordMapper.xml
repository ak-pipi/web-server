<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.niuma.admin.mapper.GameRegionalRecordMapper">

    <resultMap id="RegionalRecordMap" type="com.niuma.admin.entity.GameRegionalRecord">
        <id column="id" property="id" />
        <result column="venue_id" property="venueId" />
        <result column="round_no" property="roundNo" />
        <result column="banker" property="banker" />
        <result column="player_id0" property="playerId0" />
        <result column="player_id1" property="playerId1" />
        <result column="player_id2" property="playerId2" />
        <result column="player_id3" property="playerId3" />
        <result column="score0" property="score0" />
        <result column="score1" property="score1" />
        <result column="score2" property="score2" />
        <result column="score3" property="score3" />
        <result column="wingold0" property="winGold0" />
        <result column="wingold1" property="winGold1" />
        <result column="wingold2" property="winGold2" />
        <result column="wingold3" property="winGold3" />
        <result column="random_seed_hash" property="randomSeedHash" />
        <result column="time" property="time" />
    </resultMap>

    <sql id="four_player_record_columns">
        `id`, `venue_id`, `round_no`, `banker`,
        `player_id0`, `player_id1`, `player_id2`, `player_id3`,
        `score0`, `score1`, `score2`, `score3`,
        `wingold0`, `wingold1`, `wingold2`, `wingold3`,
        `random_seed_hash`, `time`
    </sql>

    <sql id="two_player_record_columns">
        `id`, `venue_id`, `round_no`, `banker`,
        `player_id0`, `player_id1`,
        null as `player_id2`, null as `player_id3`,
        `score0`, `score1`,
        null as `score2`, null as `score3`,
        `wingold0`, `wingold1`,
        null as `wingold2`, null as `wingold3`,
        `random_seed_hash`, `time`
    </sql>

    <sql id="four_player_filter">
        `time` <![CDATA[>=]]> #{cutoff}
        and (`player_id0` = #{playerId}
         or `player_id1` = #{playerId}
         or `player_id2` = #{playerId}
         or `player_id3` = #{playerId})
    </sql>

    <sql id="two_player_filter">
        `time` <![CDATA[>=]]> #{cutoff}
        and (`player_id0` = #{playerId}
         or `player_id1` = #{playerId})
    </sql>

    <sql id="scope_player_ids">
        <foreach collection="playerIds" item="scopePlayerId" open="(" separator="," close=")">
            #{scopePlayerId}
        </foreach>
    </sql>

    <sql id="agency_stats_player_ids">
        <foreach collection="playerIds" item="agencyStatPlayerId" open="(" separator="," close=")">
            #{agencyStatPlayerId}
        </foreach>
    </sql>

    <sql id="four_player_admin_filter">
        `time` <![CDATA[>=]]> #{cutoff}
        <if test="playerId != null and playerId != ''">
            and (`player_id0` = #{playerId}
             or `player_id1` = #{playerId}
             or `player_id2` = #{playerId}
             or `player_id3` = #{playerId})
        </if>
        <if test="playerIds != null and playerIds.size() > 0">
            and (`player_id0` in <include refid="scope_player_ids"/>
             or `player_id1` in <include refid="scope_player_ids"/>
             or `player_id2` in <include refid="scope_player_ids"/>
             or `player_id3` in <include refid="scope_player_ids"/>)
        </if>
    </sql>

    <sql id="two_player_admin_filter">
        `time` <![CDATA[>=]]> #{cutoff}
        <if test="playerId != null and playerId != ''">
            and (`player_id0` = #{playerId}
             or `player_id1` = #{playerId})
        </if>
        <if test="playerIds != null and playerIds.size() > 0">
            and (`player_id0` in <include refid="scope_player_ids"/>
             or `player_id1` in <include refid="scope_player_ids"/>)
        </if>
    </sql>

    <select id="countTaojiangMahjongRecord" resultType="Integer">
        select count(*) from `game_taojiang_mahjong_record`
        where <include refid="four_player_filter"/>
    </select>

    <select id="getTaojiangMahjongRecords" resultMap="RegionalRecordMap">
        select <include refid="four_player_record_columns"/>
        from `game_taojiang_mahjong_record`
        where <include refid="four_player_filter"/>
        order by `id` desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="countAdminTaojiangMahjongRecord" resultType="Integer">
        select count(*) from `game_taojiang_mahjong_record`
        where <include refid="four_player_admin_filter"/>
    </select>

    <select id="getAdminTaojiangMahjongRecords" resultMap="RegionalRecordMap">
        select <include refid="four_player_record_columns"/>
        from `game_taojiang_mahjong_record`
        where <include refid="four_player_admin_filter"/>
        order by `id` desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="getTaojiangMahjongRecord" resultMap="RegionalRecordMap">
        select <include refid="four_player_record_columns"/>
        from `game_taojiang_mahjong_record`
        where `id` = #{id}
    </select>

    <select id="getTaojiangMahjongPlayback" resultType="String">
        select `playback` from `game_taojiang_mahjong_record` where `id` = #{id}
    </select>

    <select id="countHongzhongMahjongRecord" resultType="Integer">
        select count(*) from `game_hongzhong_mahjong_record`
        where <include refid="four_player_filter"/>
    </select>

    <select id="getHongzhongMahjongRecords" resultMap="RegionalRecordMap">
        select <include refid="four_player_record_columns"/>
        from `game_hongzhong_mahjong_record`
        where <include refid="four_player_filter"/>
        order by `id` desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="countAdminHongzhongMahjongRecord" resultType="Integer">
        select count(*) from `game_hongzhong_mahjong_record`
        where <include refid="four_player_admin_filter"/>
    </select>

    <select id="getAdminHongzhongMahjongRecords" resultMap="RegionalRecordMap">
        select <include refid="four_player_record_columns"/>
        from `game_hongzhong_mahjong_record`
        where <include refid="four_player_admin_filter"/>
        order by `id` desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="getHongzhongMahjongRecord" resultMap="RegionalRecordMap">
        select <include refid="four_player_record_columns"/>
        from `game_hongzhong_mahjong_record`
        where `id` = #{id}
    </select>

    <select id="getHongzhongMahjongPlayback" resultType="String">
        select `playback` from `game_hongzhong_mahjong_record` where `id` = #{id}
    </select>

    <select id="countPaodekuaiRecord" resultType="Integer">
        select count(*) from `game_paodekuai_record`
        where <include refid="two_player_filter"/>
    </select>

    <select id="getPaodekuaiRecords" resultMap="RegionalRecordMap">
        select <include refid="two_player_record_columns"/>
        from `game_paodekuai_record`
        where <include refid="two_player_filter"/>
        order by `id` desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="countAdminPaodekuaiRecord" resultType="Integer">
        select count(*) from `game_paodekuai_record`
        where <include refid="two_player_admin_filter"/>
    </select>

    <select id="getAdminPaodekuaiRecords" resultMap="RegionalRecordMap">
        select <include refid="two_player_record_columns"/>
        from `game_paodekuai_record`
        where <include refid="two_player_admin_filter"/>
        order by `id` desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="getPaodekuaiRecord" resultMap="RegionalRecordMap">
        select <include refid="two_player_record_columns"/>
        from `game_paodekuai_record`
        where `id` = #{id}
    </select>

    <select id="getPaodekuaiPlayback" resultType="String">
        select `playback` from `game_paodekuai_record` where `id` = #{id}
    </select>

    <select id="countChangshaMahjongRecord" resultType="Integer">
        select count(*) from `game_changsha_mahjong_record`
        where <include refid="four_player_filter"/>
    </select>

    <select id="getChangshaMahjongRecords" resultMap="RegionalRecordMap">
        select <include refid="four_player_record_columns"/>
        from `game_changsha_mahjong_record`
        where <include refid="four_player_filter"/>
        order by `id` desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="countAdminChangshaMahjongRecord" resultType="Integer">
        select count(*) from `game_changsha_mahjong_record`
        where <include refid="four_player_admin_filter"/>
    </select>

    <select id="getAdminChangshaMahjongRecords" resultMap="RegionalRecordMap">
        select <include refid="four_player_record_columns"/>
        from `game_changsha_mahjong_record`
        where <include refid="four_player_admin_filter"/>
        order by `id` desc
        limit #{offset}, #{pageSize}
    </select>

    <select id="getChangshaMahjongRecord" resultMap="RegionalRecordMap">
        select <include refid="four_player_record_columns"/>
        from `game_changsha_mahjong_record`
        where `id` = #{id}
    </select>

    <select id="getChangshaMahjongPlayback" resultType="String">
        select `playback` from `game_changsha_mahjong_record` where `id` = #{id}
    </select>

    <sql id="player_game_record_union">
        select 1021 as `gameType`, '麻将' as `gameName`, `id`, `venue_id` as `venueId`, `round_no` as `roundNo`,
               `time`, `playback`,
               case
                 when `player_id0` = #{playerId} then ifnull(`win_gold0`, 0)
                 when `player_id1` = #{playerId} then ifnull(`win_gold1`, 0)
                 when `player_id2` = #{playerId} then ifnull(`win_gold2`, 0)
                 when `player_id3` = #{playerId} then ifnull(`win_gold3`, 0)
                 else 0
               end as `delta`
        from `game_mahjong_record`
        where `player_id0` = #{playerId} or `player_id1` = #{playerId} or `player_id2` = #{playerId} or `player_id3` = #{playerId}
        union all
        select 1031 as `gameType`, '桃江麻将' as `gameName`, `id`, `venue_id` as `venueId`, `round_no` as `roundNo`,
               `time`, `playback`,
               case
                 when `player_id0` = #{playerId} then ifnull(`wingold0`, 0)
                 when `player_id1` = #{playerId} then ifnull(`wingold1`, 0)
                 when `player_id2` = #{playerId} then ifnull(`wingold2`, 0)
                 when `player_id3` = #{playerId} then ifnull(`wingold3`, 0)
                 else 0
               end as `delta`
        from `game_taojiang_mahjong_record`
        where `player_id0` = #{playerId} or `player_id1` = #{playerId} or `player_id2` = #{playerId} or `player_id3` = #{playerId}
        union all
        select 1032 as `gameType`, '红中麻将' as `gameName`, `id`, `venue_id` as `venueId`, `round_no` as `roundNo`,
               `time`, `playback`,
               case
                 when `player_id0` = #{playerId} then ifnull(`wingold0`, 0)
                 when `player_id1` = #{playerId} then ifnull(`wingold1`, 0)
                 when `player_id2` = #{playerId} then ifnull(`wingold2`, 0)
                 when `player_id3` = #{playerId} then ifnull(`wingold3`, 0)
                 else 0
               end as `delta`
        from `game_hongzhong_mahjong_record`
        where `player_id0` = #{playerId} or `player_id1` = #{playerId} or `player_id2` = #{playerId} or `player_id3` = #{playerId}
        union all
        select 1033 as `gameType`, '跑得快' as `gameName`, `id`, `venue_id` as `venueId`, `round_no` as `roundNo`,
               `time`, `playback`,
               case
                 when `player_id0` = #{playerId} then ifnull(`wingold0`, 0)
                 when `player_id1` = #{playerId} then ifnull(`wingold1`, 0)
                 else 0
               end as `delta`
        from `game_paodekuai_record`
        where `player_id0` = #{playerId} or `player_id1` = #{playerId}
        union all
        select 1034 as `gameType`, '长沙麻将' as `gameName`, `id`, `venue_id` as `venueId`, `round_no` as `roundNo`,
               `time`, `playback`,
               case
                 when `player_id0` = #{playerId} then ifnull(`wingold0`, 0)
                 when `player_id1` = #{playerId} then ifnull(`wingold1`, 0)
                 when `player_id2` = #{playerId} then ifnull(`wingold2`, 0)
                 when `player_id3` = #{playerId} then ifnull(`wingold3`, 0)
                 else 0
               end as `delta`
        from `game_changsha_mahjong_record`
        where `player_id0` = #{playerId} or `player_id1` = #{playerId} or `player_id2` = #{playerId} or `player_id3` = #{playerId}
    </sql>

    <select id="sumAgencyGameStatsByPlayerIds" resultType="com.niuma.admin.dto.AgencyGameStatDTO">
        select `records`.`playerId` as `playerId`,
               ifnull(sum(`records`.`scoreDelta`), 0) as `totalScoreDelta`,
               count(*) as `roundCount`
        from (
            select convert(`player_id0` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`win_gold0`, 0) as `scoreDelta`
            from `game_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id0` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id1` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`win_gold1`, 0) as `scoreDelta`
            from `game_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id1` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id2` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`win_gold2`, 0) as `scoreDelta`
            from `game_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id2` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id3` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`win_gold3`, 0) as `scoreDelta`
            from `game_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id3` in <include refid="agency_stats_player_ids"/>

            union all
            select convert(`player_id0` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold0`, 0) as `scoreDelta`
            from `game_taojiang_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id0` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id1` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold1`, 0) as `scoreDelta`
            from `game_taojiang_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id1` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id2` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold2`, 0) as `scoreDelta`
            from `game_taojiang_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id2` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id3` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold3`, 0) as `scoreDelta`
            from `game_taojiang_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id3` in <include refid="agency_stats_player_ids"/>

            union all
            select convert(`player_id0` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold0`, 0) as `scoreDelta`
            from `game_hongzhong_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id0` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id1` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold1`, 0) as `scoreDelta`
            from `game_hongzhong_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id1` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id2` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold2`, 0) as `scoreDelta`
            from `game_hongzhong_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id2` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id3` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold3`, 0) as `scoreDelta`
            from `game_hongzhong_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id3` in <include refid="agency_stats_player_ids"/>

            union all
            select convert(`player_id0` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold0`, 0) as `scoreDelta`
            from `game_paodekuai_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id0` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id1` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold1`, 0) as `scoreDelta`
            from `game_paodekuai_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id1` in <include refid="agency_stats_player_ids"/>

            union all
            select convert(`player_id0` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold0`, 0) as `scoreDelta`
            from `game_changsha_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id0` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id1` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold1`, 0) as `scoreDelta`
            from `game_changsha_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id1` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id2` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold2`, 0) as `scoreDelta`
            from `game_changsha_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id2` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id3` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold3`, 0) as `scoreDelta`
            from `game_changsha_mahjong_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id3` in <include refid="agency_stats_player_ids"/>

            union all
            select convert(`player_id0` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold0`, 0) as `scoreDelta`
            from `game_yiyang_waihuzi_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id0` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id1` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold1`, 0) as `scoreDelta`
            from `game_yiyang_waihuzi_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id1` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id2` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold2`, 0) as `scoreDelta`
            from `game_yiyang_waihuzi_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id2` in <include refid="agency_stats_player_ids"/>

            union all
            select convert(`player_id0` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold0`, 0) as `scoreDelta`
            from `game_yuanjiang_qianfen_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id0` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id1` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold1`, 0) as `scoreDelta`
            from `game_yuanjiang_qianfen_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id1` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id2` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold2`, 0) as `scoreDelta`
            from `game_yuanjiang_qianfen_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id2` in <include refid="agency_stats_player_ids"/>
            union all
            select convert(`player_id3` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`wingold3`, 0) as `scoreDelta`
            from `game_yuanjiang_qianfen_record`
            where `time` &gt;= #{startTime} and `time` &lt; #{endTime}
              and `player_id3` in <include refid="agency_stats_player_ids"/>

            union all
            select convert(`t1`.`player_id` using utf8mb4) collate utf8mb4_0900_ai_ci as `playerId`, ifnull(`t1`.`win_gold`, 0) as `scoreDelta`
            from `game_lackey_round_player` `t1`
            inner join `game_lackey_round` `t2` on `t1`.`round_id` = `t2`.`id`
            where `t2`.`time` &gt;= #{startTime} and `t2`.`time` &lt; #{endTime}
              and `t1`.`player_id` in <include refid="agency_stats_player_ids"/>
        ) `records`
        group by `records`.`playerId`
    </select>

    <select id="summarizePlayerGameRecords" resultType="java.util.HashMap">
        select `gameType`, `gameName`,
               count(*) as `totalRounds`,
               sum(case when `delta` &gt; 0 then 1 else 0 end) as `winCount`,
               sum(case when `delta` &lt; 0 then 1 else 0 end) as `loseCount`,
               sum(`delta`) as `totalScoreDelta`,
               sum(case when `time` &gt;= #{todayStart} then 1 else 0 end) as `todayRounds`,
               max(`time`) as `latestTime`
        from (
            <include refid="player_game_record_union"/>
        ) records
        group by `gameType`, `gameName`
        order by `totalRounds` desc, `gameType` asc
    </select>

    <select id="getRecentPlayerGameRecords" resultType="java.util.HashMap">
        select `gameType`, `gameName`, `id`, `venueId`, `roundNo`, `time`, `delta`,
               case when `playback` is not null and `playback` != '' then true else false end as `hasReplay`
        from (
            <include refid="player_game_record_union"/>
        ) records
        where `time` &gt;= #{cutoff}
        order by `time` desc, `id` desc
        limit #{limit}
    </select>
</mapper>
