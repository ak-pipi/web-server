<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.niuma.admin.mapper.GameRuleVersionMapper">
    <select id="findEffectiveByGameId" resultType="com.niuma.admin.entity.GameRuleVersion">
        SELECT * FROM `game_rule_version`
        WHERE `game_id` = #{gameId} AND `status` = 1
        ORDER BY `effective_at` DESC
        LIMIT 1
    </select>

    <select id="listByGameId" resultType="com.niuma.admin.entity.GameRuleVersion">
        SELECT * FROM `game_rule_version`
        WHERE `game_id` = #{gameId}
        ORDER BY `version_no` DESC, `create_time` DESC
    </select>

    <select id="findGrayEffective" resultType="com.niuma.admin.entity.GameRuleVersion">
        SELECT * FROM `game_rule_version`
        WHERE `game_id` = #{gameId} AND `status` = 1 AND `gray_type` IS NOT NULL
        ORDER BY `create_time` DESC
        LIMIT 1
    </select>
</mapper>
